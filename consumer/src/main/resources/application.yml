spring:
  application:
    name: consumer
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9094}
    consumer:
      group-id: ${KAFKA_CONSUMER_GROUP_ID:konductor-consumer}
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: com.konductor.consumer.kafka.message
        spring.json.value.default.type: com.konductor.consumer.kafka.message.ProjectedEventMessage
        spring.json.use.type.headers: false
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.add.type.headers: false

konductor:
  consumer:
    name: ${KONDUCTOR_CONSUMER_NAME:sample-consumer}
  kafka:
    subscription-topic: ${KONDUCTOR_SUBSCRIPTION_TOPIC:konductor.subscription.sub_local}
    consumer-acks-topic: ${KONDUCTOR_CONSUMER_ACKS_TOPIC:konductor.consumer-acks}

server:
  port: 8086

management:
  endpoints:
    web:
      exposure:
        include: health,info
